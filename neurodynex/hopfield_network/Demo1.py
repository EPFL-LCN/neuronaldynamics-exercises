import hf_plot_tools as hfplt

# Demo1: 2 parameters: overlap and nrOfPatterns

# set the initial state to a pattern with LOW OVERLAP
# hfplt.deleteThis(initially_flipped_pixels=6, nr_random_patterns=1)
# hfplt.deleteThis(initially_flipped_pixels=7, nr_random_patterns=1)

# now play with patterns
hfplt.deleteThis(initially_flipped_pixels=4, nr_random_patterns=4)
# hfplt.deleteThis(initially_flipped_pixels=2, nr_random_patterns=1)
